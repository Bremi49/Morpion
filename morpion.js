

document.addEventListener("DOMContentLoaded", () => {
    const displayTurn = () => {
        const turn = Array.from(document.querySelectorAll("#container button"));
        const player = ["X", "O"];
        let count = 0;
        turn.forEach(button => player )
        addEventListener('click',player)
}
                    displayTurn()
})
